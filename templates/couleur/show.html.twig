{% extends 'faadmin.html.twig' %}

{% block body %}
    <div class="card-body">
        <h1>Supprimer une couleur</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Couleur</th>
                    <td>{{ couleur.codecouleur }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_couleur_index') }}" class="btn btn-primary m-1 col-2">Retour à la liste</a>

        <a href="{{ path('app_couleur_edit', {'id': couleur.id}) }}" class="btn btn-primary m-1 col-2">Modifier</a>

        {{ include('couleur/_delete_form.html.twig') }}
    </div>
{% endblock %}
