{% extends 'faadmin.html.twig' %}

{% block body %}
    <div class="card-body">
        <h1>Supprimer une forme</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Forme</th>
                    <td>{{ type.forme }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_type_index') }}" class="btn btn-primary m-1 col-2">Retour à la liste</a>

        <a href="{{ path('app_type_edit', {'id': type.id}) }}" class="btn btn-primary m-1 col-2">Modifier</a>

        {{ include('type/_delete_form.html.twig') }}
    </div>
{% endblock %}
