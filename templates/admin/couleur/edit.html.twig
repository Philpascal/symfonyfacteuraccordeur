{% extends 'faadmin.html.twig' %}

{% block body %}
    <div class="card-body">
        <h1>Modifier une couleur</h1>

        {{ include('admin/couleur/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <a href="{{ path('app_couleur_index') }}" class="btn btn-primary m-1 col-2">Retour à la liste</a>

        {{ include('admin/couleur/_delete_form.html.twig') }}
    </div>
{% endblock %}
