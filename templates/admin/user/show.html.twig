{% extends 'faadmin.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <div class="card-body">
        <h1>Utilisateur</h1>

        <table class="table">
            <tbody>
                {# <tr>
                    <th>Id</th>
                    <td>{{ user.id }}</td>
                </tr> #}
                <tr>
                    <th>Nom</th>
                    <td>{{ user.nom }}</td>
                </tr>
                <tr>
                    <th>Prenom</th>
                    <td>{{ user.prenom }}</td>
                </tr>
                <tr>
                    <th>Roles</th>
                    <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
                </tr>
                <tr>
                    <th>Tel</th>
                    <td>{{ user.tel }}</td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td>{{ user.email }}</td>
                </tr>
                <tr>
                    <th>Password</th>
                    <td>{{ user.password }}</td>
                </tr>
                {# <tr>
                    <th>IsVerified</th>
                    <td>{{ user.isVerified ? 'Yes' : 'No' }}</td>
                </tr> #}
            </tbody>
        </table>

        <a href="{{ path('app_admin_user_index') }}" class="btn btn-primary m-1 col-2">Retour à la liste</a>

        <a href="{{ path('app_admin_user_edit', {'id': user.id}) }}" class="btn btn-primary m-1 col-2">Modifier l'utilisateur</a>

        {{ include('admin/user/_delete_form.html.twig') }}
    </div>
{% endblock %}
