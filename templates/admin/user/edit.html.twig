{% extends 'faadmin.html.twig' %}

{% block body %}
    <div class="card-body">
        <h1>Modifier l'utilisateur</h1>

        {{ include('admin/user/_form.html.twig', {'button_label': 'Valider les modifications'}) }}

        <a href="{{ path('app_admin_user_index') }}" class="btn btn-primary m-1 col-2">Retour à la liste</a>

        {# {{ include('admin/user/_delete_form.html.twig') }} #}
    </div>
{% endblock %}
