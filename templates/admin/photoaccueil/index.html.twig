{% extends 'faadmin.html.twig' %}

{% block body %}
    <div class="card-body">
        <h1 class="indexphotoaccueiltitre">Images &amp; Vidéo de la page d'accueil</h1>

        <table class="table">
            
            <tbody>
            {% for photoaccueil in photoaccueils %}
                <tr class="zztop">
                    <th>Carrouselp</th>
                    <td><img src="{{app_cdn}}{{photoaccueil.carrouselp}}" alt="#" width="160"></td>
                    <th>Carrouseld</th>
                    <td><img src="{{app_cdn}}{{photoaccueil.carrouseld}}" alt="#" width="160"></td>
                    <th>Carrouselt</th>
                    <td><img src="{{app_cdn}}{{photoaccueil.carrouselt}}" alt="#" width="160"></td>
                    <th>Carrouselq</th>
                    <td><img src="{{app_cdn}}{{photoaccueil.carrouselq}}" alt="#" width="160"></td>
                </tr>
                <tr>
                    <th>Carrouselc</th>
                    <td><img src="{{app_cdn}}{{photoaccueil.carrouselc}}" alt="#" width="160"></td>
                    <th>Prestation</th>   
                    <td><img src="{{app_cdn}}{{photoaccueil.prestation}}" alt="#" width="160"></td>
                    <th>Pianod</th>
                    <td><img src="{{app_cdn}}{{photoaccueil.pianod}}" alt="#" width="160"></td>
                    <th>Pianoaq</th>
                    <td><img src="{{app_cdn}}{{photoaccueil.pianoaq}}" alt="#" width="160"></td>
                </tr>
                <tr>
                    <th>Diapason</th>
                    <td><img src="{{app_cdn}}{{photoaccueil.diapason}}" alt="#" width="160"></td>
                    <th>Accessoir</th>
                    <td><img src="{{app_cdn}}{{photoaccueil.accessoir}}" alt="#" width="160"></td>
                    <th>Clef</th>
                    <td><img src="{{app_cdn}}{{photoaccueil.clef}}" alt="#" width="160"></td>
                    <th>Vidéo</th>
                    <td>{{ photoaccueil.video }}</td>
                </tr>
                <tr>
                    <th>Action</th>
                    <td>
                        <a href="{{ path('app_photoaccueil_edit', {'id': photoaccueil.id}) }}" class="btn btn-primary m-1">Modifier la page</a>
                    </td>
                </tr>
                
            {% else %}
                <tr>
                    <td colspan="14">no records found</td>
                </tr>
            {% endfor %}
            
            </tbody>
        </table>
        
    </div>
{% endblock %}
