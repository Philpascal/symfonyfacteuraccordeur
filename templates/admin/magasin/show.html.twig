{% extends 'faadmin.html.twig' %}

{% block body %}
    <div class="card-body">
        <h1>Supprimer un magasin</h1>

        <table class="table">
            <tbody>
                {# <tr>
                    <th>Id</th>
                    <td>{{ magasin.id }}</td>
                </tr> #}
                <tr>
                    <th>Nom</th>
                    <td>{{ magasin.nom }}</td>
                </tr>
                <tr>
                    <th>Numero</th>
                    <td>{{ magasin.numero }}</td>
                </tr>
                <tr>
                    <th>Voie</th>
                    <td>{{ magasin.voie }}</td>
                </tr>
                <tr>
                    <th>Codepostal</th>
                    <td>{{ magasin.codepostal }}</td>
                </tr>
                <tr>
                    <th>Ville</th>
                    <td>{{ magasin.ville }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_magasin_index') }}" class="btn btn-primary m-1 col-2">Retour à la liste</a>

        <a href="{{ path('app_magasin_edit', {'id': magasin.id}) }}" class="btn btn-primary m-1 col-2">Modifier</a>

        {{ include('admin/magasin/_delete_form.html.twig') }}
    </div>
{% endblock %}
