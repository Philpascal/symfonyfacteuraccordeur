{% extends 'faadmin.html.twig' %}

{% block body %}
    <div class="card-body">
        <h1>Supprimer une marque</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Nom</th>
                    <td>{{ marque.nom }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_marque_index') }}" class="btn btn-primary m-1 col-2">Retour à la liste</a>

        <a href="{{ path('app_marque_edit', {'id': marque.id}) }}" class="btn btn-primary m-1 col-2">Mofidier la marque</a>

        {{ include('admin/marque/_delete_form.html.twig') }}
    </div>
{% endblock %}
