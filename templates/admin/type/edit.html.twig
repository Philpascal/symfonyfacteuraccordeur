{% extends 'faadmin.html.twig' %}

{% block body %}
    <div class="card-body">
        <h1>Modifier une forme</h1>

        {{ include('admin/type/_form.html.twig', {'button_label': 'Sauvegarder'}) }}

        <a href="{{ path('app_type_index') }}" class="btn btn-primary m-1 col-2">Retour à la liste</a>

        {{ include('admin/type/_delete_form.html.twig') }}
    </div>
{% endblock %}
